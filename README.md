# ETL Homeworks - Alexandr Petrov
Домашнее задание 1 по ETL. Тема«Источники данных».

Airflow поднял с помощью докера.

Источниками сделал таблицы (аналогично вебинару) с помощью DBevar.

2 Dag: для json - extract (файл extract_json), для xml - extraxct_xml extract_xml (файл extract_xml)

Уже в конце дз понял, что даги можно было создать в одном файле, но оставил как есть, тк работает все равно корректно.

Отчет со скринами выполнения в файле Петров_АВ_ETL_ДЗ1.pdf
